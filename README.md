# Darkweb-Intelligence-Framework-
The project proposes an AI-powered system to monitor the dark web. It uses Tor-based crawling to collect data like text, links, and screenshots. Using NLP tools, it extracts entities, detects crypto wallets, analyzes sentiment, and classifies sites. A threat scoring system generates real-time alerts and visualizes data.
